# Implementation Plans

Generated by the `improve` skill on 2026-09-17. These plans describe the Masonry component as it existed at commit `e38b7195` with local, uncommitted Masonry changes present. Execute in the order below. Read the full plan, honor its STOP conditions, and update its status row when complete.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Safely handle a missing Masonry root ref | P1 | S | — | BLOCKED — isolated implementation awaits repository-wide verification |
| 002 | Recalculate when root layout styling changes | P1 | S | 001 | BLOCKED — isolated implementation awaits registry generation |
| 003 | Keep the pre-hydration row gap aligned | P2 | M | 002 | BLOCKED — isolated implementation awaits registry generation |
| 004 | Batch Masonry measurements and writes | P2 | M | 001, 002, 003 | BLOCKED — isolated implementation awaits repository-wide verification |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- Plan 001 establishes the safe effect entry point used by all subsequent changes.
- Plan 002 should land before layout behavior is optimized or documented further, so dynamic style changes have one explicit invalidation path.
- Plan 003 and Plan 004 both touch layout mechanics; run Plan 003 first so the final batching work preserves the chosen row-gap contract.
- Component tests remain intentionally deferred: `AGENTS.md` prohibits adding `*.test.tsx` / `*.spec.tsx` for components unless explicitly requested. Each plan records the characterization coverage to add if that approval is later given.

## Execution record

- An isolated executor applied the planned Masonry changes in `/private/tmp/shark-ui-masonry-improve-exec` without touching this worktree, stash, branch switch, commit, merge, or push.
- Scoped formatting and `git diff --check` passed. `pnpm registry:build` is blocked before Masonry output generation because `registry/manifest/use-chat-helper.ts` is missing, which is outside these plans. Repository-wide lint and typecheck also report or encounter unrelated baseline failures; they do not establish the required clean verification gate.

## Findings considered and rejected

- `removeStaleColumns()` uses `items.includes()` inside a loop: this is potentially quadratic, but it is not worth an isolated change while forced layout work is the dominant confirmed cost.
- Accessibility, manifest dependencies, and current static documentation: no separate, evidence-backed defect was found in the audited scope.
