---
title: Presence
description: Animate components on mount and unmount.
links:
  doc: https://ark-ui.com/docs/components/presence
---

import manifest from "@/registry/manifest/presence";

<ComponentPreview componentName="presence" />

## Installation

<CodeTabs>
<TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @shark/presence
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>Install the following dependencies:</Step>

<RegistryDependencies dependencies={manifest.dependencies} />

<Step>Copy and paste the following code into your project.</Step>

  <ComponentSource title="components/ui/presence.tsx" src="/presence.tsx" />

<Step>Update the import paths to match your project setup.</Step>
</Steps>

</TabsContent>
</CodeTabs>

## Usage

```tsx
import React from "react";
import { Presence } from "@/registry/react/components/presence";

const [visible, setVisible] = React.useState(false);

<Presence present={visible}>
  Content that animates in and out
</Presence>
```

## Lazy

`lazyMount` and `unmountOnExit`: mount on first open, unmount on close.

<ComponentPreview
  autoHeight
  componentName="presence"
  fileName="example-lazy"
/>

## Hide mode

When `unmountOnExit` is `false`, `hideMode` chooses how closed content is hidden:

- `"display-none"`: HTML `hidden`. Effects stay alive.
- `"activity"`: React 19 Activity. Effects pause.

Ignored when `unmountOnExit` is `true`.

<ComponentPreview
  autoHeight
  componentName="presence"
  fileName="example-hide-mode"
/>

## States

Sets `data-state` to `open` or `closed` for enter and exit styles.

<ComponentPreview
  autoHeight
  componentName="presence"
  fileName="example-state"
/>

## API Reference

### Presence

| Prop | Type | Default |
| --- | --- | --- |
| `present` | `boolean` | `false` |
| `unmountOnExit` | `boolean` | `true` |
| `lazyMount` | `boolean` | `true` |
| `hideMode` | `"display-none" \| "activity"` | `"display-none"` |
| `onEnterComplete` | `() => void` | - |
| `onExitComplete` | `() => void` | - |
| `asChild` | `boolean` | `false` |

---

For a complete list of props, see the [Ark UI documentation](https://ark-ui.com/docs/utilities/presence#api-reference).
