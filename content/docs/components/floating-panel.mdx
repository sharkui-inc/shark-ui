---
title: Floating Panel
description: Content in a floating window.
seo:
  description: Draggable floating window for tools and inspectors.
links:
  doc: https://ark-ui.com/docs/components/floating-panel
  api: https://ark-ui.com/docs/components/floating-panel#api-reference
---

import manifest from "@/registry/manifest/floating-panel";

<ComponentPreview componentName="floating-panel" />

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">
  
```bash
npx shadcn@latest add @shark/floating-panel
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>This component depends on [Button](/docs/components/button) and [Scroll Area](/docs/components/scroll-area). Install them first if you haven't already.</Step>

<Step>Install the following dependencies:</Step>

<RegistryDependencies dependencies={manifest.dependencies} />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource title="components/ui/floating-panel.tsx" src="/floating-panel.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Anatomy

```text
FloatingPanel
├── FloatingPanelTrigger
└── FloatingPanelContent
    ├── FloatingPanelDragTrigger
    ├── FloatingPanelHeader
    │   ├── FloatingPanelControl
    │   │   ├── FloatingPanelMinimize
    │   │   ├── FloatingPanelMaximize
    │   │   └── FloatingPanelRestore
    │   ├── FloatingPanelTitle
    │   ├── FloatingPanelResizeTrigger
    │   ├── FloatingPanelStageTrigger
    │   └── FloatingPanelCloseTrigger
    ├── FloatingPanelBody
    └── FloatingPanelFooter
```

## Usage

```tsx
import { 
  FloatingPanel,
  FloatingPanelTrigger,
  FloatingPanelContent,
  FloatingPanelHeader,
  FloatingPanelTitle,
  FloatingPanelBody,
} from "@/components/ui/floating-panel";
```

```tsx
<FloatingPanel>
  <FloatingPanelTrigger />
  <FloatingPanelContent>
    <FloatingPanelHeader>
      <FloatingPanelTitle />
    </FloatingPanelHeader>
    <FloatingPanelBody />
  </FloatingPanelContent>
</FloatingPanel>
```

## Examples

### Controlled Size

Use `size` and `onSizeChange` to control the panel size.

<ComponentPreview componentName="floating-panel" fileName="example-controlled-size" />

### Size Limits

Use `minSize` and `maxSize` to limit the width and height of a resizable panel.

<ComponentPreview componentName="floating-panel" fileName="example-size-limits" />

### Controlled Position

Use `position` and `onPositionChange` to control the panel position.

<ComponentPreview componentName="floating-panel" fileName="example-controlled-position" />

### Custom spacing

The `[--space:--spacing("value")]` controls the internal spacing.

<ComponentPreview componentName="floating-panel" fileName="example-custom-spacing" />

> You can use breakpoint utilities to change the internal spacing at different screen sizes.

```css
  md:[--space:--spacing(6)] lg:[--space:--spacing(8)]
```

## RTL

To enable RTL support, see the [RTL configuration guide](/docs/rtl).

<RTLComponentPreview componentName="floating-panel" />

## API Reference

### FloatingPanel

Root component. Manages panel position and drag state.

| Prop | Type | Default |
| --- | --- | --- |
| `open` | `boolean` | `-` |
| `defaultOpen` | `boolean` | `false` |
| `onOpenChange` | `(details: OpenChangeDetails) => void` | `-` |
| `position` | `Point` | `-` |
| `defaultPosition` | `Point` | `-` |
| `onPositionChange` | `(details: PositionChangeDetails) => void` | `-` |
| `size` | `Size` | `-` |
| `defaultSize` | `Size` | `-` |
| `minSize` | `Size` | `-` |
| `maxSize` | `Size` | `-` |
| `onSizeChange` | `(details: SizeChangeDetails) => void` | `-` |
| `lazyMount` | `boolean` | `true` |
| `persistRect` | `boolean` | `-` |
| `unmountOnExit` | `boolean` | `true` |
| `closeOnEscape` | `boolean` | `true` |
| `initialFocusEl` | `() => MaybeElement` | `-` |
| `finalFocusEl` | `() => MaybeElement` | `-` |
| `restoreFocus` | `boolean` | `true` |
| `draggable` | `boolean` | `true` |
| `resizable` | `boolean` | `true` |
| `className` | `string` | `-` |

### FloatingPanelTrigger

Opens the floating panel on click.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### FloatingPanelContent

Holds the panel content.

| Prop | Type | Default |
| --- | --- | --- |
| `resizable` | `boolean` | `true` |
| `className` | `string` | `-` |

| Prop | Type | Default |
| --- | --- | --- |
| `--space` | `string` | `--spacing(4)` |

### FloatingPanelHeader

Header area. Acts as drag handle for repositioning.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |

### FloatingPanelTitle

Title for the panel.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### FloatingPanelBody

Scrollable body content. 

| Prop | Type | Default |
| --- | --- | --- |
| `scrollFade` | `boolean` | `true` |
| `className` | `string` | `-` |

### FloatingPanelCloseTrigger

Closes the panel on click.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### FloatingPanelRootProvider

Provides a FloatingPanel API created with `useFloatingPanel` to descendant components.

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `UseFloatingPanelReturn` | **required** |

### FloatingPanelDragTrigger

Public FloatingPanelDragTrigger component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### FloatingPanelControl

Public FloatingPanelControl component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### FloatingPanelMinimize

Public FloatingPanelMinimize component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### FloatingPanelMaximize

Public FloatingPanelMaximize component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### FloatingPanelRestore

Public FloatingPanelRestore component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### FloatingPanelResizeTrigger

Public FloatingPanelResizeTrigger component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### FloatingPanelStageTrigger

Public FloatingPanelStageTrigger component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### FloatingPanelFooter

Footer area for actions. Put **Cancel first in the DOM**, then the primary action. Uses `flex-col-reverse` / `sm:flex-row sm:justify-end` so Confirm sits on the right (and on top on mobile).

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

---

For a complete list of props, see the [Ark UI documentation](https://ark-ui.com/docs/components/floating-panel#api-reference).
