---
title: Slider
description: Select a value from a range.
seo:
  description: Range slider for continuous or stepped values.
links:
  doc: https://ark-ui.com/docs/components/slider
  api: https://ark-ui.com/docs/components/slider#api-reference
---

import manifest from "@/registry/manifest/slider";

<ComponentPreview componentName="slider" />

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">
  
```bash
npx shadcn@latest add @shark/slider
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>This component depends on [Field](/docs/components/field). Install it first if you haven't already.</Step>

<Step>Install the following dependencies:</Step>

<RegistryDependencies dependencies={manifest.dependencies} />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource title="components/ui/slider.tsx" src="/slider.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { Slider } from "@/components/ui/slider";
```

```tsx
<Slider />
```

## Controlled

Control the value with `value` and `onValueChange` for a controlled slider.

<ComponentPreview componentName="slider" fileName="example-controlled" />

## States

### Disabled

Use the `disabled` prop to disable the slider.

<ComponentPreview componentName="slider" fileName="example-disabled" />

## Examples

### With label and value

<ComponentPreview componentName="slider" fileName="example-with-label" />

### Range

Use an array with two values for a range slider.

<ComponentPreview componentName="slider" fileName="example-range" />

### Vertical

Use `orientation="vertical"` for a vertical slider.

<ComponentPreview componentName="slider" fileName="example-vertical" />

### Min and max

Set `min` and `max` to constrain the slider's value range.

<ComponentPreview componentName="slider" fileName="example-min-max" />

### Marks

The `showMarkers` and `markerInterval` props display markers and control the interval between them.

<ComponentPreview componentName="slider" fileName="example-marks" />

### Step

Set `step` to control the slider's granularity.

Use with `showMarkers` and `markerLabels` to show custom labels on the markers.

<ComponentPreview componentName="slider" fileName="example-step" />

### Large step

Focus the thumb, then hold `Shift` + Arrow keys.

<ComponentPreview componentName="slider" fileName="example-large-step" />

### Thumb collision

Use `thumbCollisionBehavior` to control what happens when thumbs meet: `none`, `push`, or `swap`.

<ComponentPreview componentName="slider" fileName="example-thumb-collision" />

## RTL

To enable RTL support, see the [RTL configuration guide](/docs/rtl).

<RTLComponentPreview componentName="slider" />

## API Reference

### Slider

Root component. Manages value, track, range, and thumb(s).

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `number[]` | `-` |
| `defaultValue` | `number[]` | `-` |
| `onValueChange` | `(details: ValueChangeDetails) => void` | `-` |
| `min` | `number` | `0` |
| `max` | `number` | `100` |
| `step` | `number` | `1` |
| `largeStep` | `number` | `10 * step` |
| `thumbCollisionBehavior` | `"none" \| "push" \| "swap"` | `-` |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` |
| `disabled` | `boolean` | `false` |
| `showMarkers` | `boolean` | `false` |
| `markerInterval` | `number` | `1` |
| `markerLabels` | `string[]` | `[]` |
| `className` | `string` | `-` |

### SliderLabel

Label for the slider. Required for accessibility.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |

### SliderValue

Shows the current value. Useful for numeric display or formatting.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |

### SliderRootProvider

Provides a Slider API created with `useSlider` to descendant components.

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `UseSliderReturn` | `required` |

---

For a complete list of props, see the [Ark UI documentation](https://ark-ui.com/docs/components/slider#api-reference).
