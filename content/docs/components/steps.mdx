---
title: Steps
description: Steps in a wizard-like format.
links:
  doc: https://ark-ui.com/docs/components/steps
  api: https://ark-ui.com/docs/components/steps#api-reference
---

import manifest from "@/registry/manifest/steps";

<ComponentPreview componentName="steps" />

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add @shark/steps
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

<RegistryDependencies dependencies={manifest.dependencies} />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource title="components/ui/steps.tsx" src="/steps.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Anatomy

```text
Steps
├── StepsList
│   └── StepsItem
│       ├── StepsTrigger
│       ├── StepsIndicator
│       ├── StepsSeparator
│       ├── StepsTitle
│       └── StepsDescription
├── StepsContent
├── StepsCompletedContent
├── StepsPrevious
└── StepsNext
```

## Usage

```tsx
import {
  Steps,
  StepsList,
  StepsItem,
  StepsTrigger,
  StepsSeparator,
  StepsIndicator,
  StepsTitle,
  StepsDescription,
  StepsContent,
  StepsCompletedContent,
  StepsNext,
  StepsPrevious
} from "@/components/ui/steps";
```

```tsx
<Steps>
  <StepsList>
    <StepsItem>
      <StepsTrigger>
        <StepsIndicator />
        <StepsTitle />
      </StepsTrigger>
      <StepsSeparator />
    </StepsItem>
  </StepsList>
  <StepsContent />
  <StepsCompletedContent />
  <StepsPrevious />
  <StepsNext />
</Steps>
```

## Examples

### Title

Use `StepsTitle` to display a description for each step.

<ComponentPreview componentName="steps" fileName="example-title" />

### Description

Use `StepsDescription` to display a description for each step.

<ComponentPreview componentName="steps" fileName="example-description" />

### Vertical

Use `orientation="vertical"` for a vertical layout.

<ComponentPreview componentName="steps" fileName="example-vertical" />

### Loading

<ComponentPreview componentName="steps" fileName="example-loading" />

### Controlled

Use the `step` and `onStepChange` props to control the active step programmatically.

<ComponentPreview componentName="steps" fileName="example-controlled" />

### Custom Icon

<ComponentPreview componentName="steps" fileName="example-icon" />

## RTL

To enable RTL support, see the [RTL configuration guide](/docs/rtl).

<RTLComponentPreview componentName="steps" />

## API Reference

### Steps

Root component. Manages step state and connectors.

| Prop | Type | Default |
| --- | --- | --- |
| `count` | `number` | `-` |
| `step` | `number` | `-` |
| `defaultStep` | `number` | `-` |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` |
| `linear` | `boolean` | `false` |
| `onStepChange` | `(details: StepChangeDetails) => void` | `-` |
| `onStepComplete` | `() => void` | `-` |
| `isStepValid` | `(details: StepValidityDetails) => boolean` | `-` |
| `isStepSkippable` | `(details: StepValidityDetails) => boolean` | `-` |
| `onStepInvalid` | `(details: StepInvalidDetails) => void` | `-` |


### StepsList

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |
Wraps the list of steps. Handles connector layout.

| Prop | Type | Default |
| --- | --- | --- |
| `--steps-gutter` | `string` | `--spacing(2)` |
| `--steps-icon-size` | `string` | `--spacing(4)` |
| `--steps-size` | `string` | `--spacing(8)` |

### StepsRootProvider

Provides a Steps API created with `useSteps` to descendant components.

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `UseStepsReturn` | **required** |

### StepsItem

Public StepsItem component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### StepsTrigger

Public StepsTrigger component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### StepsIndicator

Public StepsIndicator component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### StepsSeparator

Public StepsSeparator component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### StepsTitle

Public StepsTitle component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### StepsDescription

Public StepsDescription component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### StepsContent

Public StepsContent component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### StepsCompletedContent

Public StepsCompletedContent component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### StepsPrevious

Public StepsPrevious component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

### StepsNext

Public StepsNext component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

---

For a complete list of props, see the [Ark UI documentation](https://ark-ui.com/docs/components/steps#api-reference).
