---
title: Bottom Navigation
description: Bottom bar for primary destinations.
seo:
  description: Fixed or floating bottom nav for primary destinations on small screens.
links:
  doc: https://ark-ui.com/docs/components/tabs
  api: https://ark-ui.com/docs/components/tabs#api-reference
---

import manifest from "@/registry/manifest/bottom-navigation";

<PreviewIframe
  className="min-h-[640px]"
  src="/view/examples/bottom-navigation/example-default"
  title="Bottom Navigation"
/>

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add @shark/bottom-navigation
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>This component depends on [Tabs](/docs/components/tabs). Install it first if you haven't already.</Step>

<Step>Install the following dependencies:</Step>

<RegistryDependencies dependencies={manifest.dependencies} />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource title="components/ui/bottom-navigation.tsx" src="/bottom-navigation.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Anatomy

```text
BottomNavigation
└── BottomNavigationList
    └── BottomNavigationItem
        ├── BottomNavigationItemIcon
        └── BottomNavigationItemLabel
```

## Usage

```tsx
import {
  BottomNavigation,
  BottomNavigationList,
  BottomNavigationItem,
  BottomNavigationItemIcon,
  BottomNavigationItemLabel,
} from "@/components/ui/bottom-navigation";
```

```tsx
<BottomNavigation defaultValue="home">
  <BottomNavigationList aria-label="Main navigation">
    <BottomNavigationItem value="home">
      <BottomNavigationItemIcon>
        <HomeIcon />
      </BottomNavigationItemIcon>
      <BottomNavigationItemLabel>Home</BottomNavigationItemLabel>
    </BottomNavigationItem>
    <BottomNavigationItem value="notifications">
      <BottomNavigationItemIcon>
        <BellIcon />
      </BottomNavigationItemIcon>
      <BottomNavigationItemLabel>Notifications</BottomNavigationItemLabel>
    </BottomNavigationItem>
  </BottomNavigationList>
</BottomNavigation>
```

## Controlled

Control the active item with `value` and `onValueChange`.

<PreviewIframe
  className="min-h-[640px]"
  src="/view/examples/bottom-navigation/example-controlled"
  title="Bottom Navigation Controlled"
/>

```tsx
const [value, setValue] = React.useState("home");

<BottomNavigation
  onValueChange={(details) => setValue(details.value)}
  value={value}
>
  <BottomNavigationList>
    <BottomNavigationItem value="home">
      <BottomNavigationItemIcon>
        <HomeIcon />
      </BottomNavigationItemIcon>
      <BottomNavigationItemLabel>Home</BottomNavigationItemLabel>
    </BottomNavigationItem>
  </BottomNavigationList>
</BottomNavigation>
```

## States

### Disabled

Disable an item with the `disabled` prop.

<PreviewIframe
  className="min-h-[640px]"
  src="/view/examples/bottom-navigation/example-disabled"
  title="Bottom Navigation Disabled"
/>

```tsx
<BottomNavigationItem disabled value="news">
  <BottomNavigationItemIcon>
    <BellIcon />
  </BottomNavigationItemIcon>
  <BottomNavigationItemLabel>News</BottomNavigationItemLabel>
</BottomNavigationItem>
```

## Variants

Use the prop `variant` to change the appearance.

### Default

<PreviewIframe
  className="min-h-[640px]"
  src="/view/examples/bottom-navigation/example-variant-default"
  title="Bottom Navigation Default"
/>

```tsx
<BottomNavigationList variant="default">
  {/* ... */}
</BottomNavigationList>
```

### Inset

<PreviewIframe
  className="min-h-[640px]"
  src="/view/examples/bottom-navigation/example-variant-inset"
  title="Bottom Navigation Inset"
/>

```tsx
<BottomNavigationList variant="inset">
  {/* ... */}
</BottomNavigationList>
```

## Examples

### Icon only

Use `aria-label` on the `BottomNavigationItem` to give the item a label for screen readers.

<PreviewIframe
  className="min-h-[640px]"
  src="/view/examples/bottom-navigation/example-icon-only"
  title="Bottom Navigation Icon Only"
/>

```tsx
<BottomNavigationItem aria-label="Home" value="home">
  <BottomNavigationItemIcon>
    <HomeIcon />
  </BottomNavigationItemIcon>
</BottomNavigationItem>
```

### With links

Use `asChild` on `BottomNavigationItem` with a `Link`.

<PreviewIframe
  className="min-h-[640px]"
  src="/view/examples/bottom-navigation/example-with-links"
  title="Bottom Navigation With Links"
/>

```tsx
<BottomNavigation value={pathname}>
  <BottomNavigationList aria-label="Main navigation">
    <BottomNavigationItem asChild value="/docs">
      <Link href="/docs">
        <BottomNavigationItemIcon>
          <BookOpenIcon />
        </BottomNavigationItemIcon>
        <BottomNavigationItemLabel>Docs</BottomNavigationItemLabel>
      </Link>
    </BottomNavigationItem>
  </BottomNavigationList>
</BottomNavigation>
```

## RTL

To enable RTL support, see the [RTL configuration guide](/docs/rtl).

<PreviewIframe
  className="min-h-[640px]"
  src="/view/examples/bottom-navigation/example-rtl"
  title="Bottom Navigation RTL"
/>

## API Reference

### BottomNavigation

Root wrapper for the bottom navigation bar.

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `string` | `-` |
| `defaultValue` | `string` | `-` |
| `onValueChange` | `(details: ValueChangeDetails) => void` | `-` |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### BottomNavigationList

Fixed bar at the bottom. Typically holds nav items.

| Prop | Type | Default |
| --- | --- | --- |
| `aria-label` | `string` | `"Bottom navigation"` |
| `variant` | `"default" \| "inset"` | `"default"` |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### BottomNavigationItem

Single navigation link or button.

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `string` | `required` |
| `disabled` | `boolean` | `false` |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### BottomNavigationItemIcon

Holds the item icon.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### BottomNavigationItemLabel

Holds the item label text.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### BottomNavigationRootProvider

Provides a BottomNavigation API created with `useBottomNavigation` to descendant components.

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `UseBottomNavigationReturn` | `required` |

---

For a complete list of props, see the [Ark UI documentation](https://ark-ui.com/docs/components/tabs#api-reference).
