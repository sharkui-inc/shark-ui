---
title: Toggle Tooltip
description: Display information on click.
links:
  doc: https://ark-ui.com/docs/components/popover
  api: https://ark-ui.com/docs/components/popover#api-reference
---

import manifest from "@/registry/manifest/toggle-tooltip";

<Alert>
  <InfoIcon />
  <AlertDescription>
  Toggle Tooltip is built on top of the [Popover](/docs/components/popover) component. Visual styles come from [Tooltip](/docs/components/tooltip).
  </AlertDescription>
</Alert>

<ComponentPreview componentName="toggle-tooltip" />

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add @shark/toggle-tooltip
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>This component depends on [Popover](/docs/components/popover) and [Tooltip](/docs/components/tooltip). Install them first if you haven't already.</Step>

<Step>Install the following dependencies:</Step>

<RegistryDependencies dependencies={manifest.dependencies} />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource title="components/ui/toggle-tooltip.tsx" src="/toggle-tooltip.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Anatomy

```text
ToggleTooltip
├── ToggleTooltipTrigger
└── ToggleTooltipContent
```

## Usage

```tsx
import {
  ToggleTooltip,
  ToggleTooltipContent,
  ToggleTooltipTrigger,
} from "@/components/ui/toggle-tooltip";
```

```tsx
<ToggleTooltip>
  <ToggleTooltipTrigger />
  <ToggleTooltipContent>
    Select to see this content.
  </ToggleTooltipContent>
</ToggleTooltip>
```

## Controlled

Control open state with `open` and `onOpenChange`.

<ComponentPreview componentName="toggle-tooltip" fileName="example-controlled" />

## Choose the right primitive

Tooltip and Toggle Tooltip share visual treatment. Choose based on how people need to reach and use the content.

| Intent | Use | Why |
| --- | --- | --- |
| Name an icon-only control or add a short, optional hint | Tooltip | It opens on hover or keyboard focus, then closes automatically. Keep the content plain text and non-interactive. |
| Offer supporting details that people may open on touch, keep open, or act on | Toggle Tooltip | It uses a Popover, opened by click, Enter, or Space. Its content can receive focus and include links or buttons. |
| Give instructions required to complete a task | Visible text, a field description, or Dialog | Do not hide required information behind an overlay. |

## Positioning

Use the `positioning` prop to change the position.

<ComponentPreview componentName="toggle-tooltip" fileName="example-side" />

## Examples

### Hide Arrow

Hide the arrow with `showArrow={false}` on `ToggleTooltipContent`.

<ComponentPreview componentName="toggle-tooltip" fileName="example-without-arrow" />

## RTL

To enable RTL support, see the [RTL configuration guide](/docs/rtl).

<RTLComponentPreview componentName="toggle-tooltip" />

## API Reference

### ToggleTooltip

Root component. Combines toggle with tooltip on click.

| Prop | Type | Default |
| --- | --- | --- |
| `positioning` | `PositioningOptions` | `{ placement: "top" }` |
| `modal` | `boolean` | `false` |
| `lazyMount` | `boolean` | `true` |
| `unmountOnExit` | `boolean` | `true` |
| `open` | `boolean` | - |
| `onOpenChange` | `(details: OpenChangeDetails) => void` | - |

### ToggleTooltipTrigger

Shows the tooltip on click. Button by default; use `asChild` for custom elements.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |
| `asChild` | `boolean` | - |

### ToggleTooltipContent

Tooltip content shown in a popover on click.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |
| `asChild` | `boolean` | - |
| `showArrow` | `boolean` | `true` |

### ToggleTooltipArrow

Optional arrow pointing to the trigger. Place inside `ToggleTooltipContent`.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |
| `asChild` | `boolean` | - |

| Prop | Type | Default |
| --- | --- | --- |
| `--arrow-background` | `string` | `var(--foreground)` |
| `--arrow-size` | `string` | `calc(1.5 * var(--spacing))` |

### ToggleTooltipRootProvider

Provides a ToggleTooltip API created with `useToggleTooltip` to descendant components.

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `UseToggleTooltipReturn` | **required** |

---

For a complete list of props, see the [Ark UI documentation](https://ark-ui.com/docs/components/popover#api-reference).
