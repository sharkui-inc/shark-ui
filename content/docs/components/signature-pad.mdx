---
title: Signature Pad
description: Capture hand-drawn signatures.
links:
  doc: https://ark-ui.com/docs/components/signature-pad
  api: https://ark-ui.com/docs/components/signature-pad#api-reference
---

import manifest from "@/registry/manifest/signature-pad";

<ComponentPreview componentName="signature-pad" />

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add @shark/signature-pad
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>This component depends on [Button](/docs/components/button). Install it first if you haven't already.</Step>

<Step>Install the following dependencies:</Step>

<RegistryDependencies dependencies={manifest.dependencies} />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource title="components/ui/signature-pad.tsx" src="/signature-pad.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { SignaturePad } from "@/components/ui/signature-pad";
```

```tsx
<SignaturePad />
```

## States

### Disabled

Use the `disabled` prop to prevent drawing.

<ComponentPreview componentName="signature-pad" fileName="example-disabled" />

### Read-only

Use the `readOnly` prop to show a signature without allowing changes.

<ComponentPreview componentName="signature-pad" fileName="example-readonly" />

## Examples

### Image preview

Use `onDrawEnd` to capture the signature as an image.

<ComponentPreview componentName="signature-pad" fileName="example-image-preview" autoHeight />

### With field

Use [Field](/docs/components/field) with `FieldLabel` and `FieldHelper`.

<ComponentPreview componentName="signature-pad" fileName="example-field" />

## RTL

To enable RTL support, see the [RTL configuration guide](/docs/rtl).

<RTLComponentPreview componentName="signature-pad" />

## API Reference

### SignaturePad

Root component. Canvas for drawing signatures. Manages strokes and export.

| Prop | Type | Default |
| --- | --- | --- |
| `disabled` | `boolean` | `false` |
| `required` | `boolean` | `false` |
| `readOnly` | `boolean` | `false` |
| `name` | `string` | - |
| `paths` | `string[]` | - |
| `defaultPaths` | `string[]` | - |
| `onDraw` | `(details: DrawDetails) => void` | - |
| `onDrawEnd` | `(details: DrawEndDetails) => void` | - |
| `className` | `string` | - |

### SignaturePadRootProvider

Provides a SignaturePad API created with `useSignaturePad` to descendant components.

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `UseSignaturePadReturn` | **required** |

---

For a complete list of props, see the [Ark UI documentation](https://ark-ui.com/docs/components/signature-pad#api-reference).
