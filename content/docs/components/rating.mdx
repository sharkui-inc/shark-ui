---
title: Rating
description: Star ratings for reviews.
seo:
  description: Star ratings with half stars and read-only mode.
links:
  doc: https://ark-ui.com/docs/components/rating-group
  api: https://ark-ui.com/docs/components/rating-group#api-reference
---

import manifest from "@/registry/manifest/rating";

<ComponentPreview componentName="rating" />

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">
  
```bash
npx shadcn@latest add @shark/rating
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the dependencies:</Step>

<RegistryDependencies dependencies={manifest.dependencies} />

<Step>Import the following variables into your CSS file:</Step>

<CodeCollapsibleWrapper>
```css
@theme inline {
  --color-warning: var(--warning);
}

:root {
  --warning: var(--color-amber-500);
}

.dark {
  --warning: var(--color-amber-500);
}
```
</CodeCollapsibleWrapper>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource title="components/ui/rating.tsx" src="/rating.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { RatingGroup } from "@/components/ui/rating";
```

```tsx
<RatingGroup />
```

## Controlled

Use `value` and `onValueChange` to control the rating.

<ComponentPreview componentName="rating" fileName="example-controlled" />

## States

### Readonly

Use the `readOnly` prop to display a rating without interaction.

<ComponentPreview componentName="rating" fileName="example-readonly" />

### Disabled

Use the `disabled` prop to prevent interaction and show a disabled state.

<ComponentPreview componentName="rating" fileName="example-disabled" />

## Examples

### Half star

Use `allowHalf` to allow half-star selections.

<ComponentPreview componentName="rating" fileName="example-half-star" />

### Count

Use the `count` prop to show more or less stars.

<ComponentPreview componentName="rating" fileName="example-count" />

### Testimonial

Display a read-only rating inside a testimonial card.

<ComponentPreview componentName="rating" fileName="example-testimonial" />

### Customize size

Use the `className` prop to override the default size of the rating indicator.

<ComponentPreview componentName="rating" fileName="example-custom-size" />

### Custom icon

Compose the rating with a different icon.

<ComponentPreview componentName="rating" fileName="example-custom-icon" />

### Custom color

Override the filled color with `className`.

<ComponentPreview componentName="rating" fileName="example-custom-color" />

## RTL

To enable RTL support, see the [RTL configuration guide](/docs/rtl).

<RTLComponentPreview componentName="rating" />

## API Reference

### Rating

Root component.

| Prop | Type | Default |
| --- | --- | --- |
| `allowHalf` | `boolean` | `false` |
| `count` | `number` | `5` |
| `icon` | `React.JSX.ElementType` | `StarIcon` |
| `defaultValue` | `number` | `-` |
| `value` | `number` | `-` |
| `onValueChange` | `(details: ValueChangeDetails) => void` | `-` |
| `readOnly` | `boolean` | `-` |
| `disabled` | `boolean` | `-` |
| `className` | `string` | `-` |

### RatingRootProvider

Provides a Rating API created with `useRating` to descendant components.

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `UseRatingReturn` | **required** |

### RatingItem

Public RatingItem component. It forwards the underlying primitive props.

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |

---

For a complete list of props, see the [Ark UI documentation](https://ark-ui.com/docs/components/rating-group#api-reference).
