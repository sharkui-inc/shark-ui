---
title: Carousel
description: Cycle through a series of items.
seo:
  description: Slide through items with next/previous controls.
links:
  doc: https://ark-ui.com/docs/components/carousel
  api: https://ark-ui.com/docs/components/carousel#api-reference
---

import manifest from "@/registry/manifest/carousel";

<ComponentPreview componentName="carousel" />

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">
  
```bash
npx shadcn@latest add @shark/carousel
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>This component depends on [Button](/docs/components/button). Install it first if you haven't already.</Step>

<Step>Install the following dependencies:</Step>

<RegistryDependencies dependencies={manifest.dependencies} />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource title="components/ui/carousel.tsx" src="/carousel.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Anatomy

```text
Carousel
├── CarouselContent
│   └── CarouselItem
├── CarouselControl
│   ├── CarouselPrevious
│   └── CarouselNext
└── CarouselIndicatorGroup
    └── CarouselIndicator
```

## Usage

```tsx
import { 
  Carousel,
  CarouselControl,
  CarouselItem,
  CarouselContent,
  CarouselIndicator,
  CarouselIndicatorGroup,
  CarouselNext,
  CarouselPrevious,
} from "@/components/ui/carousel"; 
```

```tsx
<Carousel>
  <CarouselControl>
    <CarouselPrevious>Previous</CarouselPrevious>
    <CarouselNext>Next</CarouselNext>
  </CarouselControl>
  <CarouselContent>
    <CarouselItem />
    <CarouselItem />
  </CarouselContent>
</Carousel>
```

## Controlled

Use `page` and `onPageChange` to control the carousel.

<ComponentPreview componentName="carousel" fileName="example-controlled" />

## Orientation

### Horizontal

<ComponentPreview componentName="carousel" fileName="example-orientation-horizontal" />

### Vertical

<ComponentPreview componentName="carousel" fileName="example-orientation-vertical" />

## Thumbnail Indicators

Render each thumbnail inside `CarouselIndicator`.

### Horizontal

<ComponentPreview componentName="carousel" fileName="example-thumbnail-indicator" />

###  Vertical

<ComponentPreview componentName="carousel" fileName="example-thumbnail-indicator-vertical" />

## Examples

### Autoplay

Use the `autoplay` prop to play automatically.

<ComponentPreview componentName="carousel" fileName="example-autoplay" />

### Loop

Use `loop` prop to make the carousel loop.

<ComponentPreview componentName="carousel" fileName="example-loop" />

### Spacing

Use the `spacing` prop to control the gap between slides.

<ComponentPreview componentName="carousel" fileName="example-spacing" />

### Slides Per Page

Use the `slidesPerPage` prop to show multiple slides at once.

<ComponentPreview componentName="carousel" fileName="example-slides-per-page" />

### Mouse Drag

Use the `allowMouseDrag` prop to enable mouse drag.

<ComponentPreview componentName="carousel" fileName="example-mouse-drag" />

## RTL

To enable RTL support, see the [RTL configuration guide](/docs/rtl).

<RTLComponentPreview componentName="carousel" />

## API Reference

### Carousel

Root component. Manages slide state and navigation.

| Prop | Type | Default |
| --- | --- | --- |
| `slideCount` | `number` | **required** |
| `allowMouseDrag` | `boolean` | `false` |
| `autoplay` | `boolean \| { delay: number }` | `false` |
| `defaultPage` | `number` | `0` |
| `loop` | `boolean` | `false` |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` |
| `page` | `number` | `-` |
| `slidesPerPage` | `number` | `1` |
| `slidesPerMove` | `number \| 'auto'` | `'auto'` |
| `spacing` | `string` | `"16px"` |
| `padding` | `string` | `-` |
| `className` | `string` | `-` |

### CarouselContent

Wraps all slides. Handles layout and transitions.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### CarouselItem

Wraps a single slide.

| Prop | Type | Default |
| --- | --- | --- |
| `index` | `number` | **required** |
| `snapAlign` | `"start" \| "center" \| "end"` | `"start"` |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### CarouselControl

Holds prev/next navigation buttons.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### CarouselPrevious

Goes to the previous slide.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `label` | `string` | `"Previous"` |

### CarouselNext

Goes to the next slide.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `label` | `string` | `"Next"` |

### CarouselIndicatorGroup

Holds slide indicator dots or pills.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### CarouselIndicator

Indicator for a slide. Click to jump to that slide.

| Prop | Type | Default |
| --- | --- | --- |
| `index` | `number` | **required** |
| `readOnly` | `boolean` | `false` |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### CarouselRootProvider

Provides a Carousel API created with `useCarousel` to descendant components.

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `UseCarouselReturn` | **required** |

---

For a complete list of props, see the [Ark UI documentation](https://ark-ui.com/docs/components/carousel#api-reference).
