---
title: Skeleton
description: Placeholder while content loads.
seo:
  description: Placeholder blocks that mirror layout while data loads.
links:
  doc: https://ark-ui.com/docs/components/skeleton
  api: https://ark-ui.com/docs/components/skeleton#api-reference
---

import manifest from "@/registry/manifest/skeleton";

<ComponentPreview componentName="skeleton" />

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">
  
```bash
npx shadcn@latest add @shark/skeleton
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

<RegistryDependencies dependencies={manifest.dependencies} />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource title="components/ui/skeleton.tsx" src="/skeleton.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { 
  Skeleton,
  SkeletonCircle,
  SkeletonText,
} from "@/components/ui/skeleton";
```

```tsx
<SkeletonCircle />
<SkeletonText />
<Skeleton />
```

## Examples


### Skeleton

| Prop | Type | Default |
| --- | --- | --- |
| `asChild` | `boolean` | `false` |
| `className` | `string` | - |
Rectangular skeleton placeholder.

<ComponentPreview componentName="skeleton" fileName="example-skeleton" />

### Circle

Circular skeleton placeholder.

<ComponentPreview componentName="skeleton" fileName="example-skeleton-circle" />

### Text

Use the `lines` prop to control the number of placeholder lines; the last line is shorter by default.

<ComponentPreview componentName="skeleton" fileName="example-skeleton-text" />

### Loading cards

<ComponentPreview componentName="skeleton" fileName="example-loading-card" />

## RTL

To enable RTL support, see the [RTL configuration guide](/docs/rtl).

<RTLComponentPreview componentName="skeleton" />

## API Reference

### Skeleton

Rectangular loading placeholder.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |
| `asChild` | `boolean` | - |

### SkeletonCircle

Circular loading placeholder.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |
| `asChild` | `boolean` | - |

### SkeletonText

Multi-line text placeholder. Last line is shorter by default to mimic text.

| Prop | Type | Default |
| --- | --- | --- |
| `lines` | `number` | `2` |
| `className` | `string` | - |
| `asChild` | `boolean` | - |

---

For a complete list of props, see the [Ark UI documentation](https://ark-ui.com/docs/components/skeleton#api-reference).

## Related

- [Spinner](/docs/components/spinner) - loading spinner
- [Progress](/docs/components/progress) - linear progress bar
- [Circular Progress](/docs/components/circular-progress) - circular progress indicator
