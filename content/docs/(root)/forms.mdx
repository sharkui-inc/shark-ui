---
title: Forms
description: Build forms with Shark Field and a form library.
links:
  doc: https://ark-ui.com/docs/guides/forms
---

import { FormsList } from "@/components/mdx-components/forms-list";

## Picking a form library

<FormsList />

## Field Context

Controls nested in `Field` pick up `disabled`, `invalid`, `required`, and `readOnly` from context.

`Field` also owns `htmlFor` and `id`. Do not set them yourself.

```tsx
import { Field } from "@/registry/react/components/field"
import { NumberInput } from "@/registry/react/components/number-input"

const Demo = () => (
  <Field disabled>
    <NumberInput>
      <NumberInputGroup>
        <NumberInputInput />
        <NumberInputIncrement />
        <NumberInputDecrement />
      </NumberInputGroup>
    </NumberInput>
  </Field>
)
```

## Input states

### Invalid

Set `invalid` on `Field`. `FieldError` only renders when `invalid` is `true`.

```tsx
import {
  Field,
  FieldLabel,
  FieldError,
} from "@/registry/react/components/field"
import { Input } from "@/registry/react/components/input"

const Demo = () => (
  <form>
    <Field invalid>
      <FieldLabel>Username</FieldLabel>
      <Input placeholder="Enter your username" />
      <FieldError>Username is required.</FieldError>
    </Field>
  </form>
)
```

### Required

Set `required` on `Field`. Add `FieldRequiredIndicator` on the label if you want a visual mark.

```tsx
import {
  Field,
  FieldLabel,
  FieldRequiredIndicator,
  FieldError,
} from "@/registry/react/components/field"
import { Input } from "@/registry/react/components/input"

export const Demo = () => (
  <form>
    <Field required>
      <FieldLabel>
        Username
        <FieldRequiredIndicator />
      </FieldLabel>
      <Input placeholder="Enter your username" />
      <FieldError>Username is required.</FieldError>
    </Field>
  </form>
)
```
